/**
 * Main entry point for the Web Chat Agent.
 * Handles user message, updates state, and returns AI reply.
 */
export declare function processVendorMessage(vendorId: string, userMessage: string): Promise<{
    reply: string;
    status: string;
}>;
//# sourceMappingURL=onboardingChat.d.ts.map